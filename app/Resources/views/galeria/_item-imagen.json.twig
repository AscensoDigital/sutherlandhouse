{
    "title"     : "{{ item.titulo }}",
    "href"      : "{{ asset(urlBase ~ '/' ~ item.url) }}",
    "type"      : "{{ item.mimeType }}",
    "thumbnail" : "{{ asset(urlBaseMin ~ '/' ~ item.urlMiniatura) }}"
}